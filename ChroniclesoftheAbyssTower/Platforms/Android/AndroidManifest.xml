<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application
		android:label="Chronicles of the Abyss Tower"
		android:allowBackup="true"
		android:icon="@mipmap/appicon"
		android:roundIcon="@mipmap/appicon_round"
		android:supportsRtl="true"
		android:hardwareAccelerated="true"
		android:largeHeap="true">
	</application>

	<!-- Network สำหรับ Cloud Backup (Google Drive) -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />

	<!-- Storage สำหรับ Backup/Restore (Android 12 ลงไป) -->
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

	<!-- Vibration สำหรับ feedback (HP loss / damage) -->
	<uses-permission android:name="android.permission.VIBRATE" />

	<!-- Wake lock สำหรับเสียง BGM ที่เล่นต่อเนื่อง -->
	<uses-permission android:name="android.permission.WAKE_LOCK" />

	<!-- ป้องกันการ install บนอุปกรณ์ที่ไม่มี touchscreen -->
	<uses-feature android:name="android.hardware.touchscreen" android:required="true" />
</manifest>
